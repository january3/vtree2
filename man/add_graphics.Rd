% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_grobs.R
\name{add_graphics}
\alias{add_graphics}
\title{Add graphics to a vtree}
\usage{
add_graphics(
  vtree,
  grobs,
  side = "b",
  frac = 0.8,
  shape = "rectangle",
  condition = NULL,
  mask = TRUE
)
}
\arguments{
\item{vtree}{an object of class vtree}

\item{grobs}{a list either of length 1 or of the same length as the
number of nodes in the vtree.}

\item{side}{on which side of the node the grob should be inserted. Can
be one of \code{l}, \code{r}, \code{t}, \code{b} (left, right, top, bottom).}

\item{frac}{fraction of space occupied by the graphics (horizontally if
side is \code{l} or \code{r}, vertically if side is \code{t} or \code{b}).}

\item{shape}{shape of the node ("rectangle" or "roundrectangle").}

\item{condition}{Condition to evaluate in the context of the nodes data
frame. Only nodes for which the result is TRUE will be assigned a
grob.}

\item{mask}{a logical vector of the same length as the number of nodes.
A graphics will be assigned to a node only if the corresponding
mask value is TRUE.}
}
\value{
a vtree object with graphics.
}
\description{
Add graphics (images, plots) to vtree nodes. The graphical object should
be a grob (grid graphics object such as a gTree), which can be easily
created from images and ggplot plots.
}
\details{
Any graphical object which can be converted to a "grob" (grid package
graphical object) can be included in a node label. The conversion is
relatively straightforward with both, images (jpeg, png etc.) and
ggplot2 plots.

For bitmaps (raster images) such as jpeg or png, use the
\code{\link[grid:rasterGrob]{grid::rasterGrob()}} function. For ggplot2 objects, use the
\code{\link[ggplot2:ggplotGrob]{ggplot2::ggplotGrob()}} function. See the vtree vignette for examples.
}
