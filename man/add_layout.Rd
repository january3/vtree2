% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layouts.R
\name{add_layout}
\alias{add_layout}
\title{Prepare a layout for plotting a vtree}
\usage{
add_layout(
  vtree,
  layout = c("regular", "proportional", "flushed", "precomputed"),
  layout_func = NULL,
  dir = "lr",
  lwidth = NA,
  lheight = NA,
  show_root = TRUE
)
}
\arguments{
\item{vtree}{A vtree object}

\item{layout}{The layout type, either "regular" or "proportional"}

\item{layout_func}{A custom layout function.}

\item{dir}{The direction of the layout, either "lr" (left to right), "rl"
(right to left), "tb" (top to bottom), or "bt" (bottom to top)}

\item{lwidth, lheight}{The width and height of the nodes, as the fraction
of the available space. If NA, a sensible preset is chosen.}

\item{show_root}{Whether to show the root node in the layout.}
}
\value{
an object of class vtree with additional columns in the nodes
and edges data frames
}
\description{
A layout function for vtree adds several columns to both the nodes and
edges data frame which specify the positions and sizes of the nodes and
edges in the plot.
}
\details{
The builtin layouts are as follows:
\itemize{
\item "regular" - a regular layout in which all nodes have the same width and
height, and the nodes are evenly spaced along the y-axis.
\item "proportional" - a layout in which the height of each node is proportional
to the number of observations in that node, and the nodes are spaced along
the y-axis according to their cumulative frequencies.
}
}
\section{Custom layouts}{


You can also provide a custom layout function. The function should take a
the following arguments: vtree, dir, lwidth, lheight, show_root. It
must return a vtree object with following additional columns in the
nodes data frame:
\itemize{
\item x, y: the coordinates of the center of the node
\item width, height: the width and height of the node
\item full_w, full_h: the width and height of the total space allocated to
the node including the margins
}

In addition, it can have the "shape" column which specifies the shape of
the node to use. It can be "rectangle" or "roundrectangle". If not
specified, the default is "roundrectangle".

In the edge data frame, the following additional columns should be added:
\itemize{
\item x1, y1: the coordinates of the start of the edge
\item x2, y2: the coordinates of the end of the edge
}
}

\examples{
vt <- vtree_from_freqtable(Titanic, Class, Sex, Survived)
add_layout(vt, layout = "regular", dir = "lr") |> tibble::as_tibble()
}
