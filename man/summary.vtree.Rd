% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vtree_methods.R
\name{summary.vtree}
\alias{summary.vtree}
\title{Show per-variable summaries of a vtree object data}
\usage{
\method{summary}{vtree}(object, ...)
}
\arguments{
\item{object}{A vtree object.}

\item{...}{Ignored}
}
\value{
A data frame with summaries (counts and frequencies) for each
level of each variable in the vtree.
}
\description{
Show per-variable summaries of the data on which a vtree object was
constructed. The summary does not change when the vtree object is modified.
}
\details{
For each variable included in a vtree object, and for all levels of that
variable, the counts and calculated frequencies of that level in the
variable are shown, as well as labels that can be used for displaying
information. The frequency calculation depends on whether the tree was
constructed with valid percentages (i.e., excluding the NAs), or with
all samples.

Summary returned by \code{summary.vtree()} is the original data summary; it
does not change when the vtree object is modified.

The returned data frame (tibble) contains the following columns:
\itemize{
\item \code{node_col}: the name of the variable
\item \code{node_val}: the level of the variable
\item \code{count}: number of samples which have this level
\item \code{freq}: frequency of this level relative to the denominator
\item \code{denom}: the denominator used to calculate the frequency
\item \code{label}: a printable label constructed from these values
}
}
