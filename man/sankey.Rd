% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sankey.R
\name{sankey}
\alias{sankey}
\alias{plot.sankey_tree}
\title{Create a Sankey tree from a vtree object}
\usage{
sankey(vtree)

\method{plot}{sankey_tree}(x, ...)
}
\arguments{
\item{vtree, x}{A vtree object.}

\item{...}{Additional arguments passed to \code{plot_vtree()}.}
}
\value{
A sankey_tree object, which is also a vtree object.
\code{plot.sankey_tree()} can be used to plot the object and returns
a \code{grid::gTree} object.
}
\description{
Sankey trees can be used to visualize data without the conditional
frequencies that are used in a standard vtree.
}
\details{
Unlike the using sankey layout for regular vtrees, this function does
not preserve the conditional frequencies of the vtree. The node
visualization shows the marginal frequencies of the variable levels -
that is, the overall frequencies of levels for each variable, equivalent
to the values produced in vtree plots with \code{legend=TRUE}.
}
\examples{
vt <- vtree_from_freqtable(Titanic, Class, Sex, Age, Survived)
sankey(vt) |> plot()
}
