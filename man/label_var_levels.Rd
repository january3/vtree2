% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary.R
\name{label_var_levels}
\alias{label_var_levels}
\title{Get a value list as character vector}
\usage{
label_var_levels(
  cases,
  vtree,
  var,
  width = 60,
  shorten = TRUE,
  sort = TRUE,
  sep = ", "
)
}
\arguments{
\item{cases}{a data frame with cases (one sample per row)}

\item{var}{a variable name from cases and vt}

\item{width}{formatting width in characters}

\item{shorten}{if a variable level occurs more than once, should it be
mentioned only once with the number of occurences appended (default
TRUE)}

\item{sort}{whether the variable levels should be sorted (default TRUE)}

\item{sep}{separator to put between the values}

\item{vt}{a vtree object}
}
\value{
a character vector of length equal to the number rows in the
nodes data frame of the vtree object
}
\description{
For each node of the tree, identify the cases that correspond to that
node and create a formatted string label listing all values of variable
\code{var} which correspond to that node.
}
\details{
Simple wrapper around \code{\link[=vtree_apply]{vtree_apply()}} to create formatted labels which
contain a list of values for each node.
}
\examples{
library(tibble)
library(dplyr)
mt <- mtcars |>
  mutate(across(c(cyl, gear, carb), as.factor)) |>
  rownames_to_column("name")
vt <- vtree(mt, cyl, gear, carb)
# car names into a label
ids <- label_var_levels(mt, vt, "name", width=60)
vt |>
  add_labels(template="sameline", root_label = "All cars") |>
  add_labels(template="sameline", mask = find_nodes(vt, leaf),
             suffix = ids) |>
  add_layout(varspace=c(root=1, cyl=1, gear=1, carb=3), lwidth=.8) |>
  plot(fontsizes = list(nodes="adaptive"))
}
